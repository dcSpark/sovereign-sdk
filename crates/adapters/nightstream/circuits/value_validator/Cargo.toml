[package]
name = "value-validator"
version = "0.0.0"
edition = "2021"
publish = false

[[bin]]
name = "value_validator"
path = "src/main.rs"

# Standalone guest project: do not inherit the repo workspace.
[workspace]

[profile.release]
panic = "abort"

[dependencies]
nightstream-sdk = { path = "../../../../../../Nightstream/crates/nightstream-sdk", default-features = false }
