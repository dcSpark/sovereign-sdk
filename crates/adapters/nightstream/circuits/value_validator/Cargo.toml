[package]
name = "value-validator"
version = "0.0.0"
edition = "2021"
publish = false

[[bin]]
name = "value_validator"
path = "src/main.rs"

# Standalone guest project: do not inherit the repo workspace.
[workspace]

[profile.release]
panic = "abort"

[dependencies]
nightstream-sdk = { git = "https://github.com/LFDT-Nightstream/Nightstream", rev = "a2b0b8a87f6d555217535545476ef09ede805dac", default-features = false }
