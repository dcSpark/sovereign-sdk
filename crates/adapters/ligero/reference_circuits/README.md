# Reference circuits (not built)

These files are **reference-only** copies of Ligero guest circuits to make it easy to review the
expected argument layout and hashing/tag conventions *without* needing to browse the Ligero repo.

Source of truth remains the Ligero-owned artifacts (e.g. `note_spend_guest.wasm`) from the
`ligero-prover` checkout.

