This mcp is designed to operate inside the privacy pool
So the concept of address should always be the address of the privacy pool
Transfers should be done inside the pool tool (it assumes the pool already have funds)